package com.civicissues.service;

import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;

import com.async.events.NotificationEvent;

import lombok.RequiredArgsConstructor;

@Service
@RequiredArgsConstructor
public class EmailService {

    private final JavaMailSender mailSender;

    public void sendComplaintCreated(NotificationEvent e) {

        SimpleMailMessage msg = new SimpleMailMessage();
        msg.setTo("citizen@email.com");
        msg.setSubject("complaint registered");
        msg.setText(e.getMessage());

       // mailSender.send(msg);
    }

    public void sendStatusChanged(NotificationEvent e) {

        SimpleMailMessage msg = new SimpleMailMessage();
        msg.setTo("citizen@email.com");
        msg.setSubject("complaint status updated");
        msg.setText(e.getMessage());

        mailSender.send(msg);
    }
}
